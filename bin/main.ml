(* let explode input = input |> String.to_seq |> List.of_seq

let rec sum a = 
  match a with
  | [] -> 0
  | x :: a -> x + sum a;;

let rec nums st nms =
  match st with
  | [] -> nms[0] * 10 + nms[1]
  | x :: ls -> if nms.length = 0 nums ls

let () = 
  let inp = open_out "input.txt" in
 *)
